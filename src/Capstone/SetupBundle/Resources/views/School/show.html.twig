{% extends '::base.html.twig' %}

{% block body -%}
    <h1>School</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Schoolid</th>
                <td>{{ entity.schoolId }}</td>
            </tr>
            <tr>
                <th>School</th>
                <td>{{ entity.school }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('school') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('school_edit', { 'id': entity.schoolId }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
