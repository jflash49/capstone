{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Questiontype list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Questiontype</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('questiontype_show', { 'id': entity.questionType }) }}">{{ entity.questionType }}</a></td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('questiontype_show', { 'id': entity.questionType }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('questiontype_edit', { 'id': entity.questionType }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('questiontype_new') }}">
                Create a new entry
            </a>
        </li>
        <li>
            <a href="{{ path('setup_homepage') }}">
			Back
            </a>
        </li>
    </ul>
    {% endblock %}
