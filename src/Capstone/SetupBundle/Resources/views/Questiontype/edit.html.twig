{% extends '::admin/layout.html.twig' %}

{% block body -%}
<div class="row">
<div class="large-12 columns">
    <h1>Questiontype edit</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('questiontype') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
</div>
</div>
{% endblock %}
