{% extends '::admin/layout.html.twig' %}

{% block body -%}
<div class="row">
<div class="large-9 push-3 columns">
    <h1>Question list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Questionid</th>
                <th>Points</th>
                <th>Difficulty</th>
                <th>Linked Questions</th>
                <th>Question</th>
                <th>Answer</th>
                <th>Most Correct Answer MCA</th>
                <th>Question Type</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('question_show', { 'id': entity.questionId }) }}">{{ entity.questionId }}</a></td>
                <td>{{ entity.points }}</td>
                <td>{{ entity.difficulty }}</td>
                <td>{{ entity.linkedQuestions }}</td>
                <td>{{ entity.question }}</td>
                <td>{{ entity.answer }}</td>
                <td>{{ entity.mostCorrectAnswer }}</td>
                <td>{{ entity.questionType }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('question_show', { 'id': entity.questionId }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('question_edit', { 'id': entity.questionId }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
 </div>
<div class="large-3 pull-9 columns">
<ul class="stack button-group ">
        <li>
            <a href="{{ path('question_new') }}" class="button">
                New
            </a>
        </li>
        <li>
            <a href="{{ path('setup_homepage') }}" class="button">
			Back
            </a>
        </li>
    </ul>
    </div>
</div>
    {% endblock %}
