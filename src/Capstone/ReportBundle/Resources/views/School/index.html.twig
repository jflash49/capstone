{% extends '::base.html.twig' %}

{% block body -%}
    <h1>School list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Schoolid</th>
                <th>School</th><th> Parish</>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('school_show', { 'id': entity.schoolId }) }}">{{ entity.schoolId }}</a></td>
                <td>{{ entity.school }}</td><td> {{ entity.parish }} </td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('school_show', { 'id': entity.schoolId }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('school_edit', { 'id': entity.schoolId }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('school_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
